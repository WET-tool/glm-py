name: windows-wheels

on:
  push:
    branches:
      - windows

jobs:
  build-wheel-windows:
    name: Build wheels on Windows 
    runs-on: windows-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      # - name: Install vcpkg
      #   run: |
      #     git clone https://github.com/Microsoft/vcpkg.git
      #     .\vcpkg\bootstrap-vcpkg.bat

      # - name: Install libraries
      #   run: .\vcpkg\vcpkg install zlib libpng freetype libgd szip curl hdf5 netcdf-c
      
      # - name: Copy libraries to standard path
      #   run: |
      #     mkdir C:\lib
      #     xcopy /E /I .\vcpkg\installed\x64-windows\lib\* C:\lib

      # - name: Copy headers to standard path
      #   run: |
      #     mkdir C:\include
      #     xcopy /E /I .\vcpkg\installed\x64-windows\include\* C:\include

      # - name: Set Environment Variables
      #   run: |
      #     echo "C_INCLUDE_PATH=C:\include" >> $GITHUB_ENV
      #     echo "LIBRARY_PATH=C:\lib" >> $GITHUB_ENV

      - name: Install packages and setup
        run: |
          choco install make
          choco install mingw
          choco install git
          choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System'
          cd ${{ github.workspace }}
          git clone --recursive https://github.com/WET-tool/glm-aed.git
    
      - name: Compile GLM for x84_64
        shell: bash
        run: |
          cd $GITHUB_WORKSPACE/glm-source/ancillary/windows/Sources
          ./build_all.sh
          cd $GITHUB_WORKSPACE/glm-source
          ./build_glm.sh
          mkdir -p $GITHUB_WORKSPACE/binaries/windows
          cd $GITHUB_WORKSPACE/glm-source/binaries/windows
          cp -r * $GITHUB_WORKSPACE/binaries/windows

      # - name: List root directory structure
      #   run: |
      #     ls "${env:GITHUB_WORKSPACE}"
      #     echo "GitHub workspace: ${env:GITHUB_WORKSPACE}"